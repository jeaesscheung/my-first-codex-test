# Cloud 任务到本地同步流程

1. 在 Cloud 任务中完成代码或文档变更。
2. 从当前工作分支创建 PR，并在 PR 描述中写清变更范围与验证结果。
3. 审核通过后将 PR Merge 到 `main`。
4. 在 Codex App 中打开对应仓库，确认当前仓库指向 `main` 或目标同步分支。
5. 执行 `git pull` 拉取远端最新提交，确保本地工作区与已合并的 `main` 保持一致。
6. 如果本地存在未提交改动，先 `git status` 检查并决定提交、暂存或放弃，再执行同步。
